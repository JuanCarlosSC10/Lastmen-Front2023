import { FiltroVeterinarioPipe } from './filtro-veterinario.pipe';

describe('FiltroVeterinarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroVeterinarioPipe();
    expect(pipe).toBeTruthy();
  });
});
